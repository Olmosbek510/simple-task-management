package com.epam.springsecurityrevise;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

@SpringBootApplication(scanBasePackages = "com.epam.springsecurityrevise")
public class SpringSecurityReviseApplication {
    public static void main(String[] args) {
        SpringApplication.run(SpringSecurityReviseApplication.class, args);
    }
}
